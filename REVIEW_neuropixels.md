# Neuropixels subsystem review — triage

_Multi-agent line-by-line sweep (8 files, ~6k LOC) → 102 verified findings. Safe fixes auto-applied; the rest are below for your decision._

## ✅ Applied (38)

**`anatomy_converter.py`**
- `237-238` Module-level default dict aliased into the function instead of copied (latent shared-mutable-state) — Changed `probe_to_hemisphere = _PROBE_TO_HEMISPHERE` to `probe_to_hemisphere = dict(_PROBE_TO_HEMISPHERE)` so the function holds a defensive copy rather than an alias of the module constant; added an explanatory comment. Pure no-op today (body only reads it) but guards against future in-function mutation corrupting the global.
- `185` Non-obvious "unknown" region fallback is uncommented — Added an inline comment above the `region = ... else "unknown"` line explaining that positions absent from the IBL map (e.g. reference/disconnected sites) become a literal "unknown" region emitted as its own key by `_runs_to_ranges`, and that downstream `find_region_by_channel` can therefore return "unknown".
- `360` --dry-run help text calls the output a "diff summary" — Reworded the --dry-run argparse help from "Print the diff summary without writing anything." to "Compute and print the regeneration counts summary without writing the converter back to disk." since the printed object is a counts summary, not a diff.

**`histology_ibl_alignment_export.py`**
- `424` Dead instance attribute self.tracks_output_path (write-only alias of brainreg_path) — Deleted `self.tracks_output_path = self.brainreg_path` in __init__. Re-verified via grep over src/ and tests/ that the name appears only on this assignment line and is never read; all consumers use self.brainreg_path.
- `473-474` write_xyz_picks docstring omits the recursive (**) glob behaviour — Reworded the Description to state the iteration uses a recursive `**` glob of brainreg_path (descending into every subdirectory) in lexicographic order of full path, matching the actual pattern at line 495. Left the glob itself unchanged (correctness change skipped).
- `591-594 / 619` remap channel lookup raises an undocumented bare KeyError on channel-count mismatch — Added a KeyError entry to the Raises section of remap_channel_ids_to_raw documenting that a per-shank JSON with fewer channel_{i} entries than the IMRO table claims raises KeyError, and that this can leave earlier shanks already rewritten on disk. Did not alter the error-raising behavior.
- `599` No comment explaining why imro_rows[1:] skips the first IMRO group (the ~imroTbl header) — Added an inline comment above the shank_indices_in_imro slice noting imro_rows[0] is the SpikeGLX ~imroTbl header group ((probe_type, n_channels)) and per-channel (channel, shank, bank, refid, elecid) rows start at index 1, hence the [1:] slice.
- `1018` _build_cluster_template_map docstring encoding formula mismatches the code (n_templates + 1 vs n_templates_max) — Changed the docstring formula from `cluster_id * (n_templates + 1) + template_id` to `cluster_id * n_templates_max + template_id`, defined n_templates_max = spike_templates.max() + 1, and noted the multiplier is chosen strictly greater than every template id so the pairing is lossless. Matches code at lines 1042-1043.
- `697-700` _load_kilosort_arrays docstring claims all arrays kept in memory, but pc_features is memmapped — Added a sentence to the Description noting pc_features is the exception: opened as a read-only memmap (mmap_mode='r') and consumed in batches by _compute_spike_depths rather than held fully in memory, since it can be the largest array.

**`histology_stack_lightsheet_volume.py`**
- `21-23` Module docstring describes Filter sidecar exclusion more narrowly than the implementation — Reworded the Filter exclusion bullet in the module docstring (now ~lines 21-24) to state that any OME-TIFF whose filename contains the substring 'Filter' is excluded (e.g. UltraII Filter000{0,1}.ome.tif), matching the substring check in _find_lavision_files at line 81.
- `26-34` Module 'Z-direction control' section omits that the first plane (Z0000) is dropped by default — Added a paragraph to the Z-direction control section noting that with the default skip_first=True the first acquisition plane (Z0000) is omitted so the written volume starts at slice 1, flagging the one-plane offset for atlas-registration plane-index bookkeeping and cross-referencing the skip_first parameter docs.
- `122-124` Invalid xy_flip validated lazily inside the per-plane loop, after output files are opened/created — Added an up-front validation of xy_flip against _XY_FLIP_OPTIONS in stack_lightsheet_volume (immediately before output_path_template is built, alongside the wavelength/placeholder checks), so an invalid value raises ValueError before any directory is created or BigTIFF opened. The lazy raise inside _apply_xy_flip is left intact as a defensive backstop.
- `269-288` Silent empty-stack output when a channel has a single Z-plane and skip_first=True — After applying files = files[1:] under skip_first, captured the pre-skip count as original_count and added a guard that raises ValueError when files becomes empty, with a message naming the channel, the original file count, and raw_path. This prevents a header-only, page-less BigTIFF being written and returned as a success.

**`histology_stitch_smartspim_tiles.py`**
- `96-104` Short/blank metadata data row raises bare IndexError instead of the descriptive ValueError — Guarded the Obj-branch return with `if len(data) > 5:` so a short/blank row after the Obj header (e.g. a trailing empty line splitting to ['']) no longer raises a bare IndexError on data[4]/data[5]. The loop now falls through to the existing descriptive `raise ValueError(... {metadata_path})`. Added an explanatory comment.
- `245-249` Bevel-weights docstring conflates the boundary ramp value with the 1e-3 floor — Reworded the _make_bevel_weights docstring to distinguish boundary positivity (which comes from the 1-based edge distance giving ramp value 1/feather_pixels, ~0.0156 at the default 64) from the 1e-3 floor (a defensive lower bound that only engages when feather_pixels > 1000).
- `271-272` Non-obvious 1-based edge-distance formula lacks explanatory comment — Added an inline comment above the row_edge_dist/col_edge_dist lines noting the `+ 1.0` on only the top/left side yields a 1-based distance-to-nearest-edge so the outermost row/column gets distance 1 (never 0) and is never fully zero-weighted; flagged as intentional, not an off-by-one.
- `514` Per-plane BigTIFF append (contiguous write) is non-obvious and uncommented — Added an inline comment at the writer.write call explaining the 2D plane is appended as the next Z slice and that contiguous=True with axes='ZYX' is what builds a single ZYX BigTIFF across loop iterations, warning not to drop contiguous=True or change the axes tag.

**`monopolar_triangulation.py`**
- `92` Dead parameter initial_z is never read and never passed by any caller — Removed `initial_z: float | None = None` from the make_initial_guess_and_bounds_3d signature and deleted its Parameters docstring entry (former lines 122-124). Re-verified via grep across src/, tests/, and notebooks/ that no caller passes it and the body never reads it (initial z is always taken from com_3d).
- `306, 327` Optimizer non-convergence is a silent failure; NaN fallback only fired on exceptions — Added an explicit `if not output.success: return (np.nan,)*4` gate after scipy.optimize.least_squares and `if not output.success: return (np.nan, np.nan, np.nan)` after scipy.optimize.minimize, each with a comment. scipy does not raise on non-convergence, so this makes the documented NaN-on-failure contract hold for non-convergence, not only hard exceptions. All 12 module tests still pass (known-source recovery tests converge with success=True).
- `270-272` Docstring: NaN-on-failure contract now covers non-convergence — Updated the module-level / function Returns prose to state failure includes both hard exceptions and a result whose success flag is False (non-convergence / max iterations), matching the new convergence gate.
- `288-293` Unrecognized optimizer returns None implicitly; not documented — Applied the doc-only minimum fix: added a `None` entry to the Returns section stating that if optimizer is not one of the two recognised values, neither branch executes and the function returns None. Did NOT add a raise (that would be a behavior/API change - reported as skipped).
- `102-105` Initial-guess docstring said 'highest-amplitude contact' but code keys off max absolute amplitude — Reworded the make_initial_guess_and_bounds_3d docstring to 'the contact with the largest absolute amplitude, multiplied by that contact's (signed) amplitude', matching ind_max = np.argmax(np.abs(wf_data)) and max_ptp = wf_data[ind_max].

**`sglx_meta_to_coords.py`**
- `743` apply_bad_channels does not clip negative channel indices, violating its silent-clip contract — Changed the bad-channel filter from `bad_channels[bad_channels < coords.n_channels]` to `bad_channels[(bad_channels >= 0) & (bad_channels < coords.n_channels)]` so negative indices are dropped instead of wrapping (via numpy fancy indexing) to clear a real channel's connected flag. This honors the docstring's promise that indices outside [0, n_channels) are silently clipped. Related tests pass.
- `640` parse_shank_map docstring references 'snsChanMap' but the function parses 'snsShankMap' — Reworded the Returns section to reference the field actually sliced: 'snsShankMap can include a trailing SYNC entry, which we exclude by anchoring on the AP count' (was 'legacy meta files sometimes write a SYNC entry in snsChanMap'). Pure documentation correction; no code change.

**`spike_quality_metrics.py`**
- `173` silhouette method passed as a tuple silently computes the FULL silhouette instead of the simplified one — Changed QM_PARAMS['silhouette'] from {'method': ('simplified',)} to {'method': 'simplified'} (a plain string). Verified against installed spikeinterface 0.104.3 (.venv/.../metrics/quality/pca_metrics.py:297-307): the dispatch does `method == 'simplified'`, so the tuple never matched and fell into the else/full-silhouette branch. The plain string now selects simplified_silhouette_score as intended. No test asserts on the tuple form.
- `168` Dead/misleading 'amplitude_extension' key in QM_PARAMS['amplitude_cv'] — Removed 'amplitude_extension': 'spike_amplitudes' from QM_PARAMS['amplitude_cv'], leaving {'average_num_spikes_per_bin': 50, 'percentiles': (5, 95), 'min_num_bins': 10}. Re-verified via grep across src/ and tests/: the key is read by nothing (amplitude_cv is in WINDOWED_AMPLITUDE_METRIC_NAMES and computed by _compute_amplitude_metrics, which reads only the three remaining keys). It was actively misleading since it named the spike_amplitudes extension the module deliberately avoids.
- `311` somatic_classifier constructor parameter is undocumented in the class docstring — Added a `somatic_classifier : dict or None, default None` entry to the class Parameters section (after shank_spacing_microns, matching constructor order) describing that it is merged onto classify_somatic's default params via the params override in _compute_template_metrics, and that None uses the built-in defaults.
- `1156` spiking_profile catalog column hardcoded to np.nan without an explanatory comment — Added an inline comment above the np.nan row in build_session_catalog noting spiking_profile is a deliberate placeholder filled by a later/downstream step and never computed here.
- `1157-1159` Non-obvious location axis swap into loc_ap/loc_ml/loc_dv lacks an explanatory comment — Added an inline comment above the loc_ap/loc_ml/loc_dv rows stating the convention: triangulation x -> ML, y -> AP, z -> DV, hence loc_ap = location[1], loc_ml = location[0], loc_dv = location[2].
- `1162` firing_rate vs firing_rate_si populated from different sources without a clarifying comment — Added an inline comment at the cross_session_info['median_fr'] row clarifying that firing_rate is the cross-session median rate while firing_rate_si (written later from quality_metrics['firing_rate']) is the SpikeInterface single-session rate.

**`spikeinterface_helpers.py`**
- `12-46` Module docstring claims "Four groups" but enumerates only three and omits compute_amplitude_cv / compute_sd_ratio — Added a fourth numbered group to the module docstring documenting compute_amplitude_cv and compute_sd_ratio as the owned single-unit ports of SpikeInterface's compute_amplitude_cv_metrics and compute_sd_ratio, reconciling the "Four groups" wording with the actual content in the existing verbose style.
- `306-309` transform_column_range docstring lacks required Parameters/Returns and omits depth_direction semantics — Expanded the one-line summary into a full Description/Parameters/Returns docstring documenting template, channel_locations, column_range (lateral +/- band in um centred on the max-ptp peak channel), depth_direction (which selects the LATERAL axis, the inverse sense of sort_template_and_locations/get_spread), and the two-array return.
- `310` column_dim is the inverse axis of the rest of the file, and max_channel_x hardcodes column 0 — Added an inline comment clarifying that column_dim is the LATERAL axis (perpendicular to depth) and the inverse of depth_dim elsewhere, plus a comment on max_channel_x noting it is the lateral coordinate of the peak channel that the band is centred on. Did NOT change the column-0 indexing (that would alter behavior for depth_direction != "y" and is a design decision).
- `322-325` sort_template_and_locations docstring lacks required Parameters/Returns — Expanded the terse summary into a full Description/Parameters/Returns docstring stating channels are reordered ascending by DEPTH (the axis chosen by depth_direction, the opposite sense from transform_column_range) and that the template is reindexed on its channel axis (axis 1) in lockstep with channel_locations rows.
- `360-364` get_exp_decay raises uncaught UnboundLocalError for unknown exp_peak_function — Added an explicit `else: fun = np.ptp` branch (matching stock SI's get_exp_decay) with a comment, so an unrecognised exp_peak_function falls back to peak-to-peak instead of leaving `fun` unbound and raising an uncaught UnboundLocalError before the try/except NaN fallback.
- `352-353` exp_decay model parameters (decay/amp0/offset) are undocumented — Added an inline comment on the nested exp_decay model defining x = distance (um) from the peak channel, amp0 = peak-channel amplitude, offset = far-field baseline, and decay = the 1/um decay constant returned as the metric (popt[0]).
- `435` Single-letter 'MM' variable is undocumented and cryptic — Added an inline comment noting MM is the per-channel peak-to-peak amplitude profile across depth that the spread is measured on (then smoothed, normalised, thresholded below). Left the name unchanged (rename is an API/readability change not required).
- `247-248` _size inner helper lacks the comment its sibling _width has — Added a one-line comment to _size noting it returns the absolute amplitude at the index and that a None index (the detector's "not found" sentinel) yields size 0.0, mirroring the comment on sibling _width.


## ⚠️ Needs your decision (10) — skipped as behavior/design changes

**`anatomy_converter.py`**
- `178-187, 281-284` Silent total-join-failure: an all-"unknown" probe block is written and counted as a successful regeneration
  - _why skipped:_ Behavior-changing and requires a design decision: the fix must choose a skip-vs-raise policy and an unknown-fraction threshold, and alters control flow (whether the block is written and n_done incremented). Not an unambiguous one-right-answer fix.
- `182-184` int() truncation (not rounding) of channel_positions can defeat the (lateral, axial) join
  - _why skipped:_ Changing int() to int(round()) on both join sides alters join semantics for non-integer floats. The docstring asserts byte-for-byte integer agreement so it is latent today, but the change could alter behavior in non-obvious cases and is not a clear-cut typo fix; left as a design decision.
- `237-238 (alias finding's other aspect)` channel_positions.npy with <2 columns or 0 rows yields IndexError or silently-empty success block
  - _why skipped:_ Adding shape/ndim validation introduces a new failure/skip path and changes behavior (new error or new skipped_reasons entry). Requires a design decision on raise-vs-skip and is not a purely safe edit.

**`histology_ibl_alignment_export.py`**
- `495-496` xyz_picks glob is recursive (leading **) and can pull stray track files from subdirectories, shifting shank numbering
  - _why skipped:_ Behavior change: switching from the recursive `**` glob to a non-recursive top-directory scan would alter which files match and the resulting shank numbering. The finding itself acknowledges recursion may be intended for nested brainreg layouts, so the correct fix is a design decision, not unambiguous. Documented the current recursive behavior instead (see applied L473-474).
- `599-603` remap shank loop uses range(max_shank+1), erroring on valid non-contiguous shank configurations
  - _why skipped:_ Behavior change requiring a design decision. Iterating sorted(set(shank_indices_in_imro)) instead of range(n_shanks) changes the shank-to-file mapping (the shank_num -> channel_locations_shank{shank_num+1} relationship) for non-contiguous shank subsets, and could silently change output file selection. Not an unambiguous fix; the intended file-numbering convention for non-contiguous shanks is unclear.

**`monopolar_triangulation.py`**
- `146-167` float32 x0 combined with float64 bounds can produce an infeasible initial guess at the bound edges
  - _why skipped:_ The fix (drop dtype='float32' on x0) changes the documented float32 return type and alters optimizer input precision, which could subtly change fit results for all callers; not latent-only because it touches the live path. Needs a design decision (drop dtype vs clip x0 into bounds vs keep). The sole production caller passes max_distance_um=1000 so it is not hit today. Skipped as a potential non-obvious behavior change.
- `242` alpha_norm can go negative for signed feature vectors, inconsistent with the least_square [0, max_alpha] bound
  - _why skipped:_ Two distinct candidate fixes (clamp alpha_norm = max(alpha_norm, 0.0) vs merely document a non-negative-input assumption) with different behavioral consequences; clamping changes the closed-form scale and thus fit output. Requires a design decision about whether a non-negative source is intended. Latent today (wf_data is np.ptp, non-negative). Skipped.
- `288-293` Unrecognized optimizer: raise ValueError instead of returning None
  - _why skipped:_ The preferred suggested_fix (raise ValueError on unknown optimizer) is an API/behavior change that could break any caller relying on the current implicit-None fallthrough. Skipped the raise; applied only the doc-only minimum (documented the None return) under applied[].

**`sglx_meta_to_coords.py`**
- `580` parse_geom_map assumes snsGeomMap value ends exactly with ')' (trailing-text would drop last channel)
  - _why skipped:_ The suggested fix replaces the positional slice `parts[1:-1]` with a content filter (`[p for p in parts[1:] if p.strip()]` or an assert). This changes parsing behavior in a non-obvious way: it would silently swallow any empty/malformed mid-list fragment rather than relying on positional structure, and the alternative assert changes a silent path into a raise. The finding itself rates this low severity and latent (parse_spikeglx_meta uses splitlines so the tail is always empty in practice), so the correct behavior is a judgment call rather than a single unambiguous fix. Skipped per the SKIP rule for behavior-changing, non-obvious fixes.

**`spike_quality_metrics.py`**
- `356` self.imro_rows / self.geom_rows are assigned but never consumed; the geom_rows comment is misleading
  - _why skipped:_ Not safe to apply unambiguously. grep confirms neither attribute is read in production code, but tests/neuropixels/test_npx_spike_quality_metrics.py:371-372 assert on extractor.imro_rows[0] and extractor.geom_rows[1], so removing the assignments (the finding's preferred fix) would break the test suite and constitutes an API/behavior change requiring a coordinated test update. The alternative (rewording the stale comment) is a non-trivial design-judgment doc rewrite about how shank labels are actually sourced (channel_shanks.npy), not a clear-cut surgical fix. Both options exceed the safe/unambiguous bar; deferring for a human decision.


## ⚡ Performance / efficiency (9) — report-only

**`histology_ibl_alignment_export.py`**
- [medium] `1225-1232` Per-channel distance matrix and full argsort recomputed for every waveform despite depending only on peak channel — In _restrict_to_nearest_channels the Manhattan distances np.sum(np.abs(channel_positions - channel_positions[peak]), axis=1) and np.argsort(distances)[:ncw] are…
  - _fix:_ Memoize keep per distinct peak channel (a dict keyed by peak; compute distances/argsort only on first sight of a peak) and keep only the per-waveform gather inside the loop. Optionally replace np.args…
- [low] `1110-1111` Memmap PC-feature batch read uses a fancy index array instead of a contiguous slice — _compute_spike_depths builds ispi = np.arange(start, stop) and reads the memmapped pc_features as pc_features[ispi, 0, :]. Because ispi is a contiguous range, t…
  - _fix:_ Use features = np.asarray(pc_features[start:stop, 0, :], dtype=np.float64); ichannels = pc_feature_ind[spike_templates[start:stop]]; spike_depths[start:stop] = np.where(...), dropping ispi entirely.

**`histology_stitch_smartspim_tiles.py`**
- [medium] `507-512` Per-plane normalization recomputes the coverage mask and allocates extra canvas-sized arrays — For every Z plane (often thousands) the normalization computes weight_sum > 0.0 twice (lines 507 and 509) and allocates three full-canvas temporaries: safe_weig…
  - _fix:_ Compute the mask once and divide in place: covered = weight_sum > 0.0; accumulator[covered] /= weight_sum[covered]; accumulator[~covered] = 0.0; stitched = np.clip(accumulator, 0, dtype_max).astype(ti…
- [low] `505` Out-of-place tile-weight multiply allocates a temporary every tile/plane in the hottest loop — tile_img * weights_2d (line 505) allocates a fresh tile-sized float32 array on every n_tiles*n_planes iteration. tile_img comes from tifffile.imread(files[z]).a…
  - _fix:_ Multiply in place: tile_img *= weights_2d; accumulator[y_slice, x_slice] += tile_img. Removes the per-iteration temporary with identical behavior.

**`monopolar_triangulation.py`**
- [low] `240-243` Redundant distance recomputation in hot objective: reuse q instead of second _data_at_3d call — estimate_distance_error_with_log_3d is the inner objective scipy.optimize.minimize evaluates repeatedly per unit. Line 240 computes q = _data_at_3d(x, y, z, 1.0…
  - _fix:_ Replace line 243 `model_data_norm = _data_at_3d(x, y, z, alpha_norm, local_contact_locations)` with `model_data_norm = alpha_norm * q`, removing one full Euclidean-distance computation per objective e…

**`sglx_meta_to_coords.py`**
- [low] `1024` augment_legacy_meta re-parses the meta file already parsed by both callers — augment_legacy_meta opens and re-parses the .meta file via parse_spikeglx_meta(meta_path) at line 1024, although both call sites already hold a parsed meta dict…
  - _fix:_ Add an optional already-parsed meta parameter, parsed only when None, and pass the existing dict from main() (line 1309) and _write_legacy_meta (line 1092). Keep the current parse as the None fallback…

**`spike_quality_metrics.py`**
- [medium] `993` Full spike-time array loaded over the network just to read its shape — In _compute_cross_session_firing_rates, loaded_spike_times = np.load(cluster_data_dir / unit_file_name) (line 993) reads the entire per-unit spike-time array, b…
  - _fix:_ Use np.load(cluster_data_dir / unit_file_name, mmap_mode='r') and read .shape[1] off the memmap (reads only the header, not the payload), or parse the header via numpy.lib.format. The mmap form is a o…
- [low] `1271-1278` Global catalog CSV read and masked twice on the run() skip path — In run()'s skip branch, self.is_session_in_catalog(catalog_path) (line 1271) does pd.read_csv(catalog_path) internally (line 1065) and derives the this_session …
  - _fix:_ Read the catalog once in run(): drop the is_session_in_catalog call there, read existing_catalog once guarded by catalog_path.exists(), compute the this_session mask once, and branch on this_session.a…

**`spikeinterface_helpers.py`**
- [medium] `366` Python list-comprehension per-channel distance should be vectorized — `channel_distances = np.array([np.linalg.norm(cl - max_channel_location) for cl in channel_locations])` (line 366) computes the distance from the peak channel t…
  - _fix:_ Replace with `channel_distances = np.linalg.norm(channel_locations - max_channel_location, axis=1)` — mathematically identical, removing per-row Python overhead.


## 🧪 Missing test coverage (47) — report-only

**`anatomy_converter.py`**
- [medium] `271-283` imec1 / left-hemisphere path never regenerated end-to-end, and _load_ibl_position_to_region's ibl_LH branch never executed — Every regeneration test uses imec0 -> 'R' -> ibl_RH (test lines 118, 125, 149-183), and the loader test (line 108) only passes 'R', exercising only the ibl_RH a…
  - _fix:_ Parametrize the loader test over ('R','ibl_RH') and ('L','ibl_LH'), AND add an end-to-end regeneration test for an imec1 probe-day (build <date>_imec1/kilosort4/channel_positions.npy plus <mouse>/<dat…
- [medium] `304-316` Pretty-print regex collapse of multi-run region lists is untested — The two-pass regex (lines 305-316) collapses a region with MULTIPLE runs (e.g. "PAG": [[0, 40], [72, 136], ...]) onto one line. The only non-dry-run test (test …
  - _fix:_ Add a non-dry-run test producing an interleaved region (e.g. position sequence A,B,A so A has two runs), then read converter_path.read_text() (raw, not json.loads) and assert it contains a literal one…
- [low] `290-317` Non-dry-run CLI path (_cli without --dry-run) untested — test_cli_dry_run_prints_summary (test line 310) only invokes _cli with --dry-run, covering the suppression branch at lines 373-374. The non-dry-run _cli path (f…
  - _fix:_ Add a test invoking _cli with the same flags but WITHOUT --dry-run; assert the converter file on disk was rewritten and the captured JSON has output == str(converter_path) and n_triples_regenerated ==…
- [low] `285-288` Fully-skipped mouse/session pruning (empty-entry guards) not directly asserted — When every probe is skipped, new_probes_entry stays empty and the session is omitted (lines 285-286); when every session is omitted the mouse is dropped (lines …
  - _fix:_ In the all-skipped test also assert summary['output'] == {} (mouse_A pruned), confirming the `if new_probes_entry:` / `if new_mouse_entry:` guards drop fully-skipped mice.
- [low] `108-114` Malformed IBL entry (missing lateral/axial/brain_region key) error path untested — _load_ibl_position_to_region indexes entry['lateral'], entry['axial'], entry['brain_region'] directly (lines 111-112; no .get, per project convention), so a cha…
  - _fix:_ Add a test writing a channel_locations.json with a channel_0 entry lacking 'axial' and assert _load_ibl_position_to_region raises KeyError.
- [low] `240-241` Missing/invalid converter JSON input error path untested — regenerate_anatomy_converter opens and json.load's converter_path (lines 240-241) with no guard; a non-existent path raises FileNotFoundError and a non-JSON fil…
  - _fix:_ Add a test asserting regenerate_anatomy_converter(converter_path=tmp_path/'missing.json', ...) raises FileNotFoundError, optionally one for malformed JSON raising json.JSONDecodeError.

**`histology_ibl_alignment_export.py`**
- [medium] `761-770` _OPTIONAL_COPIES present-and-copied branch (line 770) is never exercised — test_copy_direct_files_copies_required_and_skips_optional only writes the _REQUIRED_COPIES sources, so _copy_direct_files always takes the skip path (lines 764-…
  - _fix:_ Extend the test (or add a sibling) to write the three _OPTIONAL_COPIES source files into the ks dir, then assert each (out / dst_name) exists after _copy_direct_files(), covering line 770.
- [low] `243-246` parse_imro_table non-integer-token (str fallback) branch is untested — All three parse_imro_table tests use only base-10 integer tokens, so the except ValueError branch at lines 245-246 that appends a non-integer token verbatim as …
  - _fix:_ Add a test parsing e.g. '(2013,LF)(abc 3 1)' and assert parsed == [[2013, 'LF'], ['abc', 3, 1]], confirming numeric tokens become int and non-numeric tokens stay stripped str.
- [low] `599-628` remap_channel_ids_to_raw second-shank rewrite is not asserted — test_remap_channel_ids_to_raw_relabels_per_shank asserts only shank1's keys ({channel_0, channel_2, origin}) and len(rewritten)==2; it never loads channel_locat…
  - _fix:_ In the test, also load channel_locations_shank2.json and assert set(shank2) == {'channel_1','channel_3'}; optionally add a non-contiguous {0,3} case to pin n_shanks behavior (see the remap correctness…
- [low] `494-508` write_xyz_picks empty-directory return ([]) is untested — The docstring (lines 491-492) promises an empty list when no matching .npy files are found, but no test points brainreg_path at a directory containing no {hemis…
  - _fix:_ Add a test pointing brainreg_path at an empty dir (or one whose .npy files start with the other hemisphere letter) and assert write_xyz_picks() == [].
- [low] `1220-1221` _restrict_to_nearest_channels ncw clamp branch is not directly asserted — When n_channels < N_CLOSEST_CHANNELS, line 1221 clamps ncw = min(...). The end-to-end test uses 4 channels so the branch executes, but no test directly asserts …
  - _fix:_ Add a 4-channel test asserting inds.shape == (n_waveforms, 4) and sparse.shape == (n_waveforms, n_samples, 4).
- [low] `401-435` Constructor override params (out_subdir, kilosort_version, histology_dirname, ephys_dirname) are untested — The constructor exposes four overrides that change resolved paths: kilosort_version feeds self.ks_path (line 427), out_subdir feeds the output leaf (line 428), …
  - _fix:_ Add a variant constructing with kilosort_version='2', out_subdir='ibl_RH_v2', histology_dirname='hist', ephys_dirname='ephys' and assert ks_path.name=='kilosort2', ephys_out_path.name=='ibl_RH_v2', an…

**`histology_stack_lightsheet_volume.py`**
- [high] `260-290` Multi-wavelength happy path (the default (488, 561) flow) is never executed — Every end-to-end test passes a single wavelength (488). The only multi-wavelength test (test_stack_lightsheet_volume_multi_wavelength_requires_placeholder, line…
  - _fix:_ Add a test writing C00 and C01 4x4 planes into a raw dir, calling stack_lightsheet_volume(raw, tmp_path/'stack_{wavelength_nm}.tif') with default wavelengths, asserting written == [tmp_path/'stack_488…
- [medium] `250-251, 275-278` Single-wavelength WITH a {wavelength_nm} placeholder (formatting branch) is untested — All single-wavelength tests pass literal output paths with no placeholder (stack_488.tif line 106, flipped.tif line 131, zflip.tif line 199), so has_placeholder…
  - _fix:_ Add a test calling stack_lightsheet_volume(raw, tmp_path/'stack_{wavelength_nm}.tif', wavelength_nm=488) and assert the returned path is tmp_path/'stack_488.tif' (placeholder substituted to integer 48…
- [low] `280` Output parent-directory creation (mkdir(parents=True)) is never exercised — Line 280 creates missing parent directories. Every test writes output directly into tmp_path (already existing), so mkdir(parents=True, exist_ok=True) always op…
  - _fix:_ Add a test passing output_path = tmp_path/'nested'/'deeper'/'stack.tif' (intermediates not pre-created) and assert the file is written, confirming nested parents were auto-created.
- [low] `235-238` Single-element wavelength iterable (the tuple-not-int else branch) is untested — Lines 235-238 branch on isinstance(wavelength_nm, int): int -> one-tuple (236); else tuple(wavelength_nm) (238). Tests cover int (488), empty iterable (line 184…
  - _fix:_ Add a test calling stack_lightsheet_volume(raw, tmp_path/'out.tif', wavelength_nm=(488,)) (single-element tuple, no placeholder) and assert it succeeds and returns [tmp_path/'out.tif'].
- [low] `81` Filter0001 sidecar exclusion and the other-channel non-match are not asserted — test_find_lavision_files_sorts_and_excludes_filter_sidecars (line 69) seeds a Filter0000 sidecar (line 80) and a C01 plane (line 81) but asserts only the two C0…
  - _fix:_ Extend the test to add a 't_UltraII_C00_Filter0001.ome.tif' sidecar and assert it is excluded, and add an assertion that _find_lavision_files(tmp_path, 'C01') returns exactly ['t_UltraII_C01_xyz-Table…

**`histology_stitch_smartspim_tiles.py`**
- [high] `434-437` Empty tile-directory error path is untested — The FileNotFoundError('No SmartSPIM tile directories found under ...') branch (lines 434-437) is never exercised; test_stitch_smartspim_tiles_validates_waveleng…
  - _fix:_ Add a test with root/metadata.txt plus an empty Ex_488_Ch0 dir (or only non-integer subdirs) asserting pytest.raises(FileNotFoundError, match='No SmartSPIM tile directories found').
- [medium] `442-445` Tile directory with zero TIFF planes error path is untested — FileNotFoundError('No TIFF planes found in tile directory ...') at lines 442-445 (tile registered in layout but containing no *.tiff) is never triggered by any …
  - _fix:_ From _make_smartspim_tree, add a valid tile dir (e.g. Ex_488_Ch0/40/40_0) with no .tiff files and assert pytest.raises(FileNotFoundError, match='No TIFF planes found in tile directory').
- [medium] `458-462` Non-2D tile plane (ndim != 2) error path is untested — ValueError('Expected 2D tile planes, got shape ...') at lines 458-462 has no coverage; only the adjacent dtype check (463-467) is tested by test_stitch_smartspi…
  - _fix:_ Overwrite all planes with a 3D uint16 array (e.g. np.zeros((2,4,4), dtype=np.uint16)) and assert pytest.raises(ValueError, match='Expected 2D tile planes').
- [medium] `497-501` Mid-stream per-plane shape-mismatch error path is untested — ValueError('Plane shape ... does not match reference ...') at lines 497-501 is the only protection against ragged tiles (reference shape taken from one plane at…
  - _fix:_ Overwrite one plane of tile B (e.g. 20/20_0/000100.tiff) with a differently-shaped array (np.zeros((5,5), dtype=np.uint16)) and assert pytest.raises(ValueError, match='does not match reference').
- [medium] `413-419, 484-518` Multi-wavelength success path (placeholder formatting + return order) is untested — Only the multi-wavelength validation error is tested (line 295). The successful two-channel branch is never run: {wavelength_nm} placeholder substitution (484-4…
  - _fix:_ Add Ex_561_Ch1 tiles, call stitch_smartspim_tiles(root, tmp_path/'stitched_{wavelength_nm}.tif', wavelength_nm=(488,561), feather_pixels=1), and assert the returned list equals [.../stitched_488.tif, …
- [low] `507-512` Fully-uncovered canvas pixel (weight_sum == 0 -> 0.0) branch is untested — The np.where(weight_sum>0, ..., 0.0) fallback at 507-512 only fires when the canvas bounding box has pixels covered by no tile (gapped/diagonal tiles). All exis…
  - _fix:_ Place two tiles with a gap (e.g. stage (0,0) and (200,0) at 1 um pixel size), stitch with feather_pixels=1, and assert the uncovered columns read back as 0 while covered columns equal their sources.
- [low] `94-104` metadata.txt with 'Obj' header as the final line is untested — The idx+1 < len(lines) boundary guard at line 96 (Obj header as the last line, no data row -> ValueError at 102-104) is uncovered; test_parse_smartspim_metadata…
  - _fix:_ Add a test whose final line is the 'Obj' header with no following data row and assert pytest.raises(ValueError, match='Could not parse pixel size').

**`monopolar_triangulation.py`**
- [medium] `139-150` Initial alpha guess x0[3] and high alpha bound (max_alpha) are never asserted — make_initial_guess_and_bounds_3d computes the source-magnitude initial guess x0[3] = dist_to_max * max_ptp (lines 139-150) and the upper alpha bound max_alpha =…
  - _fix:_ Add a test with a non-uniform wf where the peak contact is known: place a single large amplitude on one contact and assert x0[3] == pytest.approx(dist_from_com_to_peak_contact * peak_amp) and high_bou…
- [low] `242` Closed-form alpha_norm scaling in the log objective is only tested at the perfect-fit (zero) point — estimate_distance_error_with_log_3d's analytic alpha step (line 242) and the normalized-MSE (line 245) are tested only at the true source where the result is ex…
  - _fix:_ Add a test evaluating estimate_distance_error_with_log_3d at an offset position and assert a hand-computed positive value, or at minimum assert err(offset) > err(true) > 0.

**`sglx_meta_to_coords.py`**
- [low] `951-957` _imro_derived_meta_fields 5-field NP1 imro entry (len!=6) branch is untested — The NP1 (type 0) path scans for filter-bypass only when len(first_entry) == 6 (line 952); a 5-field per-channel entry skips the scan and any_full stays 'false'.…
  - _fix:_ Add a test feeding a 5-field NP1 imro table, e.g. _imro_derived_meta_fields({'imroTbl': '(0,384)(0 0 0 250 450)(1 0 0 250 450)'}), asserting full == 'imAnyChanFullBand=false\n' (scan skipped because l…
- [low] `661-667` parse_shank_map linear-probe (equal even/odd x-offset) branch is untested — parse_shank_map applies a staggered offset via np.where(even_rows, x + x_offset_even_um, x + x_offset_odd_um) at lines 663-667. The only parse_shank_map test (l…
  - _fix:_ Add a parse_shank_map test with a linear-offset probe (imDatPrb_pn='NP1015', both offsets 27, h-pitch 32) and snsShankMap entries spanning even and odd rows, asserting both rows get x = col*32 + 27 re…
- [low] `1018-1019` augment_legacy_meta documented FileNotFoundError path is untested — augment_legacy_meta documents 'Raises FileNotFoundError if meta_path does not exist' (lines 1018-1019), reached via parse_spikeglx_meta(meta_path) at line 1024.…
  - _fix:_ Add a test: with pytest.raises(FileNotFoundError): augment_legacy_meta(tmp_path / 'absent.meta', some_coords).
- [low] `1210-1232` plot_coords multi-shank loop (n_shanks > 1) is untested — plot_coords loops over geometry.n_shanks (line 1210) with per-shank x_offset = shank * shank_pitch_um (line 1211) and on_this_shank masking (line 1222), produci…
  - _fix:_ Add a plot_coords test with a four-shank geom map (e.g. NP2010), asserting len(fig.axes[0].collections) == 2 * n_shanks.

**`spike_quality_metrics.py`**
- [medium] `518-520` Sentinel (-1) coercion to None in _compute_template_metrics is untested — Lines 518-520 coerce stock SI's -1 'no extremum found' sentinel to None for trough_index/peak_before_index/peak_after_index so the downstream is-None guards fir…
  - _fix:_ Add a test with a degenerate single-channel template whose trough sits one sample from the end (so get_trough_and_peak_idx returns peak_after_index == -1), and assert the dependent metrics are NaN rat…
- [low] `1000-1002` Multi-session hs_sn list / collapse branches untested in _compute_cross_session_firing_rates — test_compute_cross_session_firing_rates (test lines 565-604) uses one session with one headstage, always hitting the collapse branch (hs_sn = hs_ids[0]). The br…
  - _fix:_ Add a test with changepoints_info containing two sessions in which the unit's .npy appears with different headstage_sn; assert hs_sn is the list [HS_a, HS_b] and median_fr is the median of the two per…
- [low] `998` Empty firing_rate_dict -> NaN median_fr path untested at the unit level — Line 998 yields median_fr = np.nan when firing_rate_dict is empty (unit absent from every session). No test drives _compute_cross_session_firing_rates with a un…
  - _fix:_ Extend test_compute_cross_session_firing_rates with a second unit whose file name is in no session; assert rec_sessions == [], hs_sn == [] and np.isnan(median_fr).
- [low] `407-411` load() unit_subset int and sequence selection branches untested — load() slices the sorting when unit_subset is set: an int selects the first N unit ids (line 409), a sequence selects exactly those ids via np.asarray (line 411…
  - _fix:_ Test load() with a stubbed sorting exposing unit_ids and select_units (or extract a tiny helper) and assert: int N -> select_units(unit_ids[:N]); list [a,c] -> select_units(np.asarray([a,c])); None ->…
- [low] `818-819` Degenerate-sparsity fallback (chan_inds.size == 0) in compute_unit_locations untested — When the same-shank intersection of the sparse set is empty, lines 818-819 fall back to chan_inds = [template_peak_ch]. test_compute_unit_locations_locks_region…
  - _fix:_ Add a case where the unit's sparsity contains only channels on a different shank than the template peak channel, forcing chan_inds.size == 0; assert the fallback runs, closest_channel equals template_…
- [low] `683` amplitude_cutoff value and the positive-median (no-flip) amplitude branch are unasserted/untested — Line 683 flips the amplitude sign when amplitude_median < 0 before feeding amplitude_cutoff. In test_compute_amplitude_metrics_returns_per_unit_frame the biphas…
  - _fix:_ Assert np.isfinite(frame.loc[0, 'amplitude_cutoff']) in the existing test, and add a second unit whose extremum-channel waveform value at nbefore is positive (positive amplitude_median) to cover the n…
- [low] `1205-1222` build_session_catalog write=False path untested — build_session_catalog(write=False) must return/store session_catalog without touching disk and without setting self.catalog_path. test_build_session_catalog_ass…
  - _fix:_ Add a test calling build_session_catalog(catalog_path=p, write=False); assert the returned frame is correct, self.session_catalog is set, self.catalog_path is unchanged (None) and not p.exists().
- [low] `1033-1036` is_session_in_catalog default-path (catalog_path=None) resolution untested — The None -> _resolve_catalog_path -> ephys_path.parent/'unit_catalog.csv' default-location branch of is_session_in_catalog is never exercised: its tests (test l…
  - _fix:_ Add a test writing unit_catalog.csv beside a tmp EPHYS subdir and calling is_session_in_catalog() with no argument; assert True, proving the None default resolves to the file build_session_catalog wri…
- [low] `504-544` _compute_template_metrics tested with a single unit whose extremum is channel 0 only — test_compute_template_metrics_populates_all_keys uses one unit with amps descending from channel 0, so extremum_channel = argmin (line 507) is always 0, and the…
  - _fix:_ Add a test with two units where unit 1's largest biphasic is on a non-zero channel and the units have different sparsity masks; assert both get all keys and that extremum selection lands on the intend…

**`spikeinterface_helpers.py`**
- [medium] `322-328` sort_template_and_locations is entirely untested — sort_template_and_locations (322-328) reorders template columns and channel_locations by depth but is exercised only indirectly inside get_spread on an already-…
  - _fix:_ Add a direct test: unsorted channel_locations (e.g. y=[30,10,20,0]) plus a template whose columns carry identifying values, call sort_template_and_locations(..., depth_direction='y'), assert loc[:,1] …
- [medium] `431` get_spread depth_direction != "y" (x/z) branch untested — Both get_spread tests (test_get_spread_is_depth_extent_above_threshold line 275, test_get_spread_applies_gaussian_smoothing line 440) call with depth_direction=…
  - _fix:_ Add a test with informative amplitude variation along x (column 0), call get_spread(..., depth_direction="x", column_range=None), and assert the returned extent matches the hand-computed x-depth span …
- [medium] `310` transform_column_range depth_direction != "y" branch (column_dim=1) untested — test_transform_column_range_restricts_to_columns_near_peak (line 380) and the get_spread tests only use the default depth_direction="y", so column_dim is always…
  - _fix:_ Add a transform_column_range test with depth_direction='x' (column_dim=1) and a column_range that retains only channels within range of the peak's y coordinate, asserting the surviving channel set.
- [low] `263-268` classify_somatic _ratio inf path produced but never asserted — _ratio returns inf when denominator is 0. The canonical-somatic test (peak_before_index=None) already drives trough_to_peak2_ratio = _ratio(5.0, 0.0) -> inf, so…
  - _fix:_ In an existing or new test add an explicit assert result['trough_to_peak2_ratio'] == float('inf') (peak_before_index=None) so the inf path is verified rather than merely traversed.
- [low] `282-284` classify_somatic width-scale branch for non-61-sample templates untested — Lines 282-284 scale min_width_first_peak / min_width_main_trough by width_scale = n_samples/standard_spike_width and apply max(2.0,...) / max(3.0,...) floors. E…
  - _fix:_ Add a test with a non-61 template length (e.g. 122) and peaks engineered so the unscaled vs scaled width threshold flips condition A, asserting the 'somatic' result depends on the scaled threshold.
- [low] `288-289` classify_somatic condition-A width gate not differentiated from ratio gates — Condition A (286-291) is a four-way AND including two width gates (main_peak_before_width < min_width_first_peak, main_trough_width < min_width_main_trough). Th…
  - _fix:_ Add a test mirroring the condition-A case but with a wide leading peak (plateau giving main_peak_before_width >= min_width_first_peak) and the ratio conjuncts still satisfied, asserting result['somati…
- [low] `389-395` get_exp_decay except-Exception NaN fallback not deterministically covered — get_exp_decay wraps curve_fit + r2 in try/except (373-395) returning NaN when curve_fit raises. test_get_exp_decay_is_nan_when_fit_is_poor (line 421) uses a fla…
  - _fix:_ Add a test with inputs that make curve_fit raise (e.g. fewer points than parameters) and assert get_exp_decay returns np.nan, covering the except branch.
- [low] `517` compute_amplitude_cv abs(mean) negative-mean case untested — Line 517 computes amp_mean = abs(np.mean(amplitudes)); all amplitude_cv tests (lines 296,320,461,475) use non-negative amplitudes, so the abs() guarding a negat…
  - _fix:_ Add a test with negative-mean amplitudes and assert the returned cv_median is finite and positive, exercising the abs() in amp_mean.
